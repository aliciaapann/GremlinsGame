package gremlins;
import processing.core.PImage;

public class SpecialFire extends Fireball{

    public SpecialFire(int x, int y, int speed, PImage sprite, Map map, Level level, int wizardVersion, Wizard wizard) {
        super(x, y, speed, sprite, map, level, wizardVersion, wizard);
    }
}
